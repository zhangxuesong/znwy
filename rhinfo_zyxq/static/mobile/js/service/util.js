define(['core', 'tpl'], function (core, tpl) {
    var modal = {};
    modal.init = function (params) {
        core.json('util/nowxapp',{}, function(ret){
			return
		});
    };
    return modal
});